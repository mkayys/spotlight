
export default class Tile {

    constructor(x, y) {
        this.pos = { "x": x, "y": y };
        this.width = 100; // not sure what dimensions the grid will be
        this.height = 100; // not sure what dimensions the grid will be
        this.isPresent = false;
    }

    show() {
        if (isPresent) {
            // show up on canvas for grid
        }
    }
}